# 产品B - 电商智能自动化平台
# Python依赖

# 核心依赖（无额外依赖，使用产品A模块）
# 产品A模块通过sys.path引入

# Web框架
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6

# 可选依赖（用于真实LLM集成）
openai>=1.0.0
anthropic>=0.5.0

# 数据库（后续添加）
sqlalchemy>=2.0.0
pymysql>=1.1.0

# 工具库
python-dotenv>=1.0.0
pydantic>=2.0.0
